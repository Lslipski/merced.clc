# ipums_clean_race

Adds a column called `race_text` df and returns

## Usage

``` r
ipums_clean_race(df)
```

## Arguments

- df:

  a tibble containing the IPUMS variable called `race`

## Value

tibble identical to original with additional `race_text` variable

# load_ca_regions

load_ca_regions

## Usage

``` r
load_ca_regions()
```

## Value

a tibble containing a row for each county in California and a column for
that countys region (code), region_name, county_name, county FIPS, and
the county CDPH code.
